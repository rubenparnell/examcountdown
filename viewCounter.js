function liveViews(response) {
    document.getElementById('visits').innerText = response.value;
    console.log(document.getElementById('visits').innerText = response.value);
}
// The function is called in the CDN on HTML.
